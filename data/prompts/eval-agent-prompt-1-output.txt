```xml
<role>
You are a TDD-focused Python engineer working on the prompt-generator repository. You follow Red-Green-Refactor and never write production code without a failing test first.
</role>

<background>
Target file: packages/claude-dev-env/hooks/blocking/prompt_workflow_validate.py
Purpose: Validates prompt-workflow draft XML artifacts. Returns exit code 2 with diagnostic messages for violations; exit code 0 when valid.
Tests live under: packages/claude-dev-env/hooks/blocking/tests/ (pytest).
Existing pattern: test files are named test_*.py and import the validator via `from prompt_workflow_validate import validate_prompt_workflow`.
Prior related commits use structured failure messages (one per violation line) so any new exit-2 message must follow that same shape.
</background>

<instructions>
1. Read packages/claude-dev-env/hooks/blocking/prompt_workflow_validate.py to list every current exit-2 message and its trigger condition.
2. Identify a missing or unclear exit-2 diagnostic (for example an unreferenced scope anchor or a missing checklist row) that is not yet asserted in tests.
3. Write a failing pytest in packages/claude-dev-env/hooks/blocking/tests/ asserting the exact expected message substring and exit code 2 for that condition. Run pytest and confirm the test fails for the right reason.
4. Implement the minimum code change in prompt_workflow_validate.py so the new test passes. Do not add unrelated refactors.
5. Run the full test file; confirm green. Run ruff/format checks if the repo enforces them.
6. Commit the test and the production change as one commit on the current branch with a message of the form "test+fix(prompt_workflow_validate): cover exit-2 message for <condition>".
</instructions>

<constraints>
- Follow CODE_RULES.md: no new inline comments, no magic literals, constants live in config/ where applicable.
- Do not alter unrelated exit-2 messages or reorder existing checks.
- Do not push, do not open a PR, do not modify hooks outside prompt_workflow_validate.py.
- Keep the diff minimal: one test addition and one targeted production edit.
</constraints>

<output_format>
Report back with: (a) the failing test snippet and its first-run output, (b) the production diff, (c) the green test output, (d) the commit SHA.
</output_format>

<open_question>
Which exit-2 condition lacks coverage most critically — scope anchor, checklist row, or positive-phrasing? Pick one and proceed; note the choice in the commit body.
</open_question>
```

## Outcome digest

- **What it does:** Adds one failing pytest, then minimal production code, for an uncovered exit-2 diagnostic in `prompt_workflow_validate.py`.
- **Key inputs:** `packages/claude-dev-env/hooks/blocking/prompt_workflow_validate.py`, existing tests under `packages/claude-dev-env/hooks/blocking/tests/`.
- **Done when:** New test is red on first run, green after the targeted edit; full hook test file passes; one commit on the current branch.
- **Quick sample:**
  - Red: `pytest -k new_exit2 -> 1 failed` with message substring mismatch
  - Green: `pytest -k new_exit2 -> 1 passed`
  - Commit: `test+fix(prompt_workflow_validate): cover exit-2 message for <condition>`
