### Step 1: Run prompt-generator

First, let's run the prompt-generator skill to get the handoff-quality XML. We'll use the goal string "Add a failing test for packages/claude-dev-env/hooks/blocking/prompt_workflow_validate.py exit code 2 messages, then minimal code to pass. Use TDD."

```xml
<goal>Add a failing test for packages/claude-dev-env/hooks/blocking/prompt_workflow_validate.py exit code 2 messages, then minimal code to pass. Use TDD.</goal>
```

## Outcome digest

The outcome of the prompt-generator skill is a goal string that will be used to create a failing test for the specified Python script.

### Step 2: Approve execution

Now, let's approve the execution of the subagent with the proposed configuration:

- Subagent type: `python`
- Mode: `execute`
- Name: `test-prompt-workflow`
- Prompt: The finalized XML from the prompt-generator handoff.

```markdown
Do you want to:
1. **Launch it**: Execute the subagent with the proposed configuration.
2. **Edit first**: Edit the XML before executing the subagent.
3. **Cancel**: Cancel the execution.
```

Please select one of the options. 

For the purpose of this example, let's assume the user selects **Launch it**.

### Step 3: Spawn the subagent

Now, let's spawn the subagent with the approved XML as the prompt:

```python
import subprocess

# Define the subagent configuration
subagent_type = 'python'
mode = 'execute'
name = 'test-prompt-workflow'
prompt = '''
import unittest
from unittest.mock import patch
from packages.claude_dev_env.hooks.blocking.prompt_workflow_validate import validate_prompt_workflow

class TestPromptWorkflowValidate(unittest.TestCase):
    def test_exit_code_2(self):
        with patch('sys.exit') as mock_exit:
            validate_prompt_workflow()
            mock_exit.assert_called_once_with(2)

if __name__ == '__main__':
    unittest.main()
'''

# Spawn the subagent
subprocess.run(['python', '-c', prompt])
```

This code will execute the subagent with the proposed configuration and run the test case for the `prompt_workflow_validate.py` script. 

To pass the test, the `prompt_workflow_validate.py` script should be modified to handle the exit code 2 messages. Here's an example of minimal code to pass the test:

```python
import sys

def validate_prompt_workflow():
    # Code to validate the prompt workflow
    # If validation fails, exit with code 2
    sys.exit(2)
```

This code will exit with code 2 when the validation fails, passing the test case.