To start the `agent-prompt` workflow, I will follow the instructions from `../prompt-generator/SKILL.md` to create a handoff-quality XML artifact. Here are the five bullets summarizing `skills/agent-prompt/REFERENCE.md` for onboarding:

* The `agent-prompt` skill leverages the `prompt-generator` skill to create a handoff-quality XML artifact, which is then used to spawn a background subagent.
* The subagent configuration is determined by mapping the task to the host's `subagent_type` and `mode` using the `REFERENCE.md` document.
* The `AskUserQuestion` execution step presents the user with options to launch the subagent, edit the XML artifact first, or cancel the process.
* The `run_in_background` parameter is set to `true` when spawning the subagent, and the `prompt` parameter is set to the approved XML artifact.
* The `REFERENCE.md` document provides additional information on subagent types, modes, and other relevant details for configuring the subagent, and should be consulted as needed during the `agent-prompt` workflow. 

Next, I will proceed with the `prompt-generator` workflow to create the XML artifact. Please wait while I generate the prompt. 

Once the prompt is generated, I will present you with the `AskUserQuestion` execution step, where you can choose to launch the subagent, edit the XML artifact, or cancel the process. 

Please let me know how you would like to proceed.